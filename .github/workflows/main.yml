name: Stidents Api Pipeline

on: [push]

jobs:
  hello:
    runs-on: ubuntu-latest
    
    steps:
      - name: "Say Hello"
        run: echo "Hello Word"
        
  listing:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repo
        use: actions/checkout@v2
      - name: List all files
        run: ls

